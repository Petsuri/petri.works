{
    "name": "site",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "install-projects": "(npm install) & (cd api-client && npm install) & (cd client && npm install) & (cd serverless && npm install)",
        "dev:client": "cd client && npm start",
        "dev:api": "cd serverless && serverless offline",
        "dev": "run-p dev:**"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
