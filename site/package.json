{
    "name": "site",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "install-projects-win": "(npm install) & (cd api-client && npm install) & (cd ../client && npm install) & (cd ../serverless && npm install) & (cd ../storage-dynamodb && npm install)",
        "install-projects": "(npm install) & (cd api-client && npm install) & (cd client && npm install) & (cd serverless && npm install) & (cd storage-dynamodb && npm install)",
        "dev:client": "cd client && npm start",
        "dev:api": "cd serverless && serverless offline",
        "dev:docker": "docker-compose up",
        "dev": "run-p dev:**"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
