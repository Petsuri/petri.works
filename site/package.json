{
  "name": "@petriworks/site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier --write \"./**/*.{js,jsx,ts,tsx,json}\"",
    "install-projects-win": "(npm install) & (cd common && npm install && tsc -b) & (cd ../api-contracts && npm install && tsc -b) & (cd ../api-client && npm install && tsc -b) & (cd ../client && npm install) & (cd ../serverless && npm install) & (cd ../storage-dynamodb && npm install && tsc -b)",
    "install-projects": "(npm install) & (cd common && npm install && tsc -b) & (cd api-contracts && npm install && tsc -b) & (cd api-client && npm install && tsc -b) & (cd client && npm install) & (cd serverless && npm install) & (cd storage-dynamodb && npm install && tsc -b)",
    "dev:build-api-client": "cd api-client && tsc -b --clean && tsc -b",
    "dev:build-common": "cd common && tsc -b --clean && tsc -b",
    "dev:build-storage-dynamodb": "cd storage-dynamodb && tsc -b --clean  && tsc -b",
    "dev:build-api-constracts": "cd api-contracts && tsc -b --clean && tsc -b",
    "dev:client": "cd client && npm start",
    "dev:dynamodb": "cd serverless && serverless dynamodb install && serverless dynamodb start",
    "dev:serverless": "cd serverless && serverless offline",
    "dev": "run-p dev:**",
    "test:api-contracts": "cd api-contracts && npm test",
    "test:client": "cd client && npm run test:nowatch",
    "test:common": "cd common && npm test",
    "test": "run-p test:**"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^2.2.1"
  },
  "prettier": {
    "tabWidth": 2,
    "singleQuote": false,
    "printWidth": 100
  }
}